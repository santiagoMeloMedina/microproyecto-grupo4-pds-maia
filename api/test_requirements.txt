-r requirements.txt

# Dependencias de prueba de la API.
# httpx lo necesita TestClient de Starlette; las versiones anteriores a 0.27 ya
# no son compatibles con las versiones actuales de FastAPI.
pytest>=8.0.0,<10.0.0
httpx>=0.27.0,<1.0.0
