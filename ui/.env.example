# URL base de la API de riesgo de retraso.
#
# En desarrollo, la API corre en el 8002 (tox run -e run desde api/).
# En el despliegue con Docker Compose, apunta al host publicado.
# Copiar a ui/.env.local y ajustar.
VITE_API_URL=http://localhost:8002
